package com.potential.goodquestion.domain.parent.repository;

import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Parent 레포지토리
 */
public interface ParentRepository extends JpaRepository<Object, Long> {
}
