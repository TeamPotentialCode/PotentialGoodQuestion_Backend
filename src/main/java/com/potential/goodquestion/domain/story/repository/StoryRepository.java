package com.potential.goodquestion.domain.story.repository;

import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Story 레포지토리
 */
public interface StoryRepository extends JpaRepository<Object, Long> {
}
