package com.potential.goodquestion.domain.child.repository;

import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Child 레포지토리
 */
public interface ChildRepository extends JpaRepository<Object, Long> {
}
